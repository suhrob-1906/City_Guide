# Database (Render Postgres will provide this)
DATABASE_URL="postgresql://user:password@localhost:5432/uzbek_city_helper"

# OpenWeather API (required for air quality)
OPENWEATHER_API_KEY="your_openweather_api_key"

# Map tiles (required for MapLibre)
# You can use the demo tiles for testing, but get a real key for production
# MapTiler: https://www.maptiler.com/
NEXT_PUBLIC_MAP_STYLE_URL="https://demotiles.maplibre.org/style.json"

# Upstash Redis (optional, for production caching)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# App URL (for CORS, optional)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
